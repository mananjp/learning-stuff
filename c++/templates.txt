template <typename T>
T add(T a, T b) {
    return a + b;
}

int main() {
    std::cout << add<int>(5, 10) << std::endl;    // Works with integers
    std::cout << add<double>(5.5, 10.1) << std::endl; // Works with doubles
    return 0;
}
