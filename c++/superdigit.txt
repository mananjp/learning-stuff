#include <iostream>
#include <vector>
#include <stack>
#include <cstring>
#include <string>
using namespace std;


int superdigit(long long n)
{
      if (n < 10) return n; 

    long long sum = 0;
    while (n > 0) {
        sum += n % 10;
        n /= 10;       
    }
    
    return superdigit(sum);
}

int superdigit(string n, int k){
   int x = stoi(n);
   x*=k;
   int a =0;
   for(int i=0 ; i<k; i++){
       
     while(x>0)
       {
            a += x%10;
            x = x/10;
           
       }
  
}
    return superdigit(a);
}


int main()
{
    string n;
    cin >> n;
    int k;
    cin>> k;
    


  int x = superdigit(n,k);
  cout << x;
}